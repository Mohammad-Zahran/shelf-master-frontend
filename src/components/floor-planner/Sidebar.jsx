import React from "react";

const Sidebar = (
  viewMode,
  setViewMode,
  setShowPopup,
  showSettings,
  setShowSettings,
  width,
  setWidth,
  height,
  setHeight,
  selectedFurnitureIndex,
  deleteFurniture
) => {
  return <div>Sidebar</div>;
};

export default Sidebar;
